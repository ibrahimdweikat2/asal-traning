version: '2.2'

services:
  groca:
    build: ./first-react-app
    container_name: Groca_c
    ports:
      - '3000:3000'
    volumes:
      - ./first-react-app:/groca
      - /groca/node_modules
    stdin_open: true
    tty: true